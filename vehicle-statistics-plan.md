# 管理端统计分析 - 车辆分析

## 一、功能概述

在管理端"统计分析"菜单下新增"车辆分析"子菜单，展示车辆相关的统计图表。

## 二、页面设计

### 2.1 页面布局

```
┌─────────────────────────────────────────────────────────┐
│  统计分析 > 车辆分析                                      │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐               │
│  │  总车辆数        │  │  可租用车辆      │               │
│  │  50             │  │  35             │               │
│  └─────────────────┘  └─────────────────┘               │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐│
│  │  车辆租赁热度排行（Top 10）                          ││
│  │  📊 柱状图                                          ││
│  └─────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐│
│  │  车辆状态分布                                        ││
│  │  🥧 饼图（可用、维修中、已租出）                      ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

### 2.2 统计卡片

| 卡片名称 | 数据来源 | 说明 |
|---------|---------|------|
| 总车辆数 | 所有未删除的车辆总数 | 实时统计 |
| 可租用车辆 | 状态为 AVAILABLE 的车辆数 | 实时统计 |

### 2.3 图表说明

#### 2.3.1 车辆租赁热度排行
- **类型**：横向柱状图
- **数据**：订单数量最多的前10辆车
- **X轴**：订单数量
- **Y轴**：车辆名称（品牌+车型）
- **排序**：按订单数降序

#### 2.3.2 车辆状态分布
- **类型**：饼图
- **数据**：各状态车辆数量占比
- **状态**：
  - 可用 (available)
  - 维修中 (maintenance)
  - 已租出 - 根据当前时间有进行中订单的车辆

## 三、后端设计

### 3.1 新增接口

| 接口 | 方法 | 路径 | 描述 |
|------|------|------|------|
| 车辆热度排行 | GET | `/api/admin/statistics/vehicle-popularity` | Top 10 热门车辆 |
| 车辆状态分布 | GET | `/api/admin/statistics/vehicle-status` | 各状态车辆数量 |
| 车辆概览 | GET | `/api/admin/statistics/vehicle-overview` | 总车辆数、可租用数 |

### 3.2 接口详情

#### 3.2.1 车辆热度排行

**请求**：
```http
GET /api/admin/statistics/vehicle-popularity
```

**响应**：
```json
{
  "code": 200,
  "data": [
    { "vehicleName": "宝马 X5", "orderCount": 28 },
    { "vehicleName": "奔驰 E300", "orderCount": 25 },
    ...
  ]
}
```

#### 3.2.2 车辆状态分布

**请求**：
```http
GET /api/admin/statistics/vehicle-status
```

**响应**：
```json
{
  "code": 200,
  "data": [
    { "status": "可用", "count": 35 },
    { "status": "维修中", "count": 5 },
    { "status": "已租出", "count": 10 }
  ]
}
```

**注意**："已租出"不是车辆状态字段，而是根据当前时间有进行中订单（paid/in_use）的车辆计算得出。

#### 3.2.3 车辆概览

**请求**：
```http
GET /api/admin/statistics/vehicle-overview
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "totalVehicles": 50,
    "availableVehicles": 35
  }
}
```

### 3.3 需新增文件

1. **VehiclePopularityDTO.java** - 车辆热度数据DTO
2. **VehicleStatusDTO.java** - 车辆状态分布DTO
3. **VehicleOverviewDTO.java** - 车辆概览DTO
4. 在 **StatisticsService.java** 中新增方法
5. 在 **StatisticsServiceImpl.java** 中实现方法
6. 在 **StatisticsController.java** 中新增接口

## 四、前端设计

### 4.1 需新增文件

1. **src/views/admin/VehicleStatistics.vue** - 车辆分析页面
2. 在 **src/api/admin/statistics.js** 中新增接口

### 4.2 路由配置

```js
{
  path: 'vehicle-statistics',
  name: 'AdminVehicleStatistics',
  component: () => import('@/views/admin/VehicleStatistics.vue'),
  meta: { title: '车辆分析' }
}
```

### 4.3 侧边栏菜单

```
统计分析
  └── 订单分析
  └── 车辆分析  ← 新增
```

## 五、开发步骤

### 5.1 后端
1. 创建 DTO 类（3个）
2. 在 StatisticsService 新增方法
3. 在 StatisticsServiceImpl 实现方法
4. 在 StatisticsController 新增接口

### 5.2 前端
1. 在 API 文件中新增接口
2. 创建车辆分析页面
3. 添加路由配置
4. 修改侧边栏菜单

## 六、注意事项

1. "已租出"状态是动态计算的，根据当前时间有进行中订单的车辆
2. 热度排行按订单数量统计，不区分订单状态（取消的订单也计入）
3. 车辆名称显示格式：品牌 + 车型
