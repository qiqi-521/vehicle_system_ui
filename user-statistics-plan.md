# 管理端统计分析 - 用户分析

## 一、功能概述

在管理端"统计分析"菜单下新增"用户分析"子菜单，展示用户相关的统计图表。

## 二、页面设计

### 2.1 页面布局

```
┌─────────────────────────────────────────────────────────┐
│  统计分析 > 用户分析                                      │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐               │
│  │  总用户数        │  │  今日新增        │               │
│  │  128            │  │  5              │               │
│  └─────────────────┘  └─────────────────┘               │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐│
│  │  用户注册趋势                    [近7天] [近30天]    ││
│  │  📈 折线图                                          ││
│  └─────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐│
│  │  用户状态分布                                        ││
│  │  🥧 饼图（正常、禁用）                                ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

### 2.2 统计卡片

| 卡片名称 | 数据来源 | 说明 |
|---------|---------|------|
| 总用户数 | 所有未删除的用户总数 | 实时统计 |
| 今日新增 | 今日注册的用户数 | 实时统计 |

### 2.3 图表说明

#### 2.3.1 用户注册趋势
- **类型**：折线图
- **数据**：按天统计新增用户数量
- **时间范围**：近7天 / 近30天（可切换）
- **X轴**：日期
- **Y轴**：新增用户数

#### 2.3.2 用户状态分布
- **类型**：饼图
- **数据**：各状态用户数量占比
- **状态**：
  - 正常 (ACTIVE)
  - 禁用 (DISABLED)

## 三、后端设计

### 3.1 新增接口

| 接口 | 方法 | 路径 | 描述 |
|------|------|------|------|
| 用户注册趋势 | GET | `/api/admin/statistics/user-trend` | 按天统计新增用户 |
| 用户状态分布 | GET | `/api/admin/statistics/user-status` | 各状态用户数量 |
| 用户概览 | GET | `/api/admin/statistics/user-overview` | 总用户数、今日新增 |

### 3.2 接口详情

#### 3.2.1 用户注册趋势

**请求**：
```http
GET /api/admin/statistics/user-trend?days=7
```

**响应**：
```json
{
  "code": 200,
  "data": [
    { "date": "2024-01-15", "count": 3 },
    { "date": "2024-01-16", "count": 5 },
    { "date": "2024-01-17", "count": 2 },
    ...
  ]
}
```

#### 3.2.2 用户状态分布

**请求**：
```http
GET /api/admin/statistics/user-status
```

**响应**：
```json
{
  "code": 200,
  "data": [
    { "status": "正常", "count": 120 },
    { "status": "禁用", "count": 8 }
  ]
}
```

#### 3.2.3 用户概览

**请求**：
```http
GET /api/admin/statistics/user-overview
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "totalUsers": 128,
    "todayNewUsers": 5
  }
}
```

### 3.3 需新增文件

1. **UserTrendDTO.java** - 用户注册趋势数据DTO
2. **UserStatusDTO.java** - 用户状态分布DTO（注意：已有VehicleStatusDTO，这个用于用户）
3. **UserOverviewDTO.java** - 用户概览DTO
4. 在 **StatisticsService.java** 中新增方法
5. 在 **StatisticsServiceImpl.java** 中实现方法
6. 在 **StatisticsController.java** 中新增接口

## 四、前端设计

### 4.1 需新增文件

1. **src/views/admin/UserStatistics.vue** - 用户分析页面
2. 在 **src/api/admin/statistics.js** 中新增接口

### 4.2 路由配置

```js
{
  path: 'user-statistics',
  name: 'AdminUserStatistics',
  component: () => import('@/views/admin/UserStatistics.vue'),
  meta: { title: '用户分析' }
}
```

### 4.3 侧边栏菜单

```
统计分析
  └── 订单分析
  └── 车辆分析
  └── 用户分析  ← 新增
```

## 五、开发步骤

### 5.1 后端
1. 创建 DTO 类（3个）
2. 在 StatisticsService 新增方法
3. 在 StatisticsServiceImpl 实现方法
4. 在 StatisticsController 新增接口

### 5.2 前端
1. 在 API 文件中新增接口
2. 创建用户分析页面
3. 添加路由配置
4. 修改侧边栏菜单

## 六、注意事项

1. 用户统计只统计未删除的用户（deleted_at 为空）
2. 今日新增按 created_at 日期筛选
3. 用户状态枚举：ACTIVE（正常）、DISABLED（禁用）
