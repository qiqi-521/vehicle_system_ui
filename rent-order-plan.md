# 立即租赁功能实现计划

## Context
用户在车辆详情页点击"立即租赁"，进入租赁订单页面，选择租赁时间、查看费用明细，确认后提交订单。

## 实现方案

### 后端接口（已有）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/user/orders` | POST | 创建订单 |
| `/api/user/orders/{id}/pay` | POST | 支付订单（模拟） |

**创建订单请求参数：**
```json
{
  "vehicleId": 1,
  "startTime": "2024-01-01 10:00:00",
  "endTime": "2024-01-03 10:00:00"
}
```

### 前端实现

**1. 新建文件**

| 文件路径 | 说明 |
|---------|------|
| `src/views/user/RentOrder.vue` | 租赁订单页面 |
| `src/api/user/order.js` | 用户端订单API |

**2. 页面内容**

- 左侧：车辆信息卡片（图片、品牌、车型、车牌号、单价）
- 右侧：租赁表单
  - 开始时间（日期+时间选择器）
  - 结束时间（日期+时间选择器）
  - 租赁天数（自动计算）
  - 费用明细（租金、押金预估）
  - 总计金额
  - 提交订单按钮

**3. 页面流程**

```
车辆详情页 → 点击"立即租赁" → 租赁订单页面 → 提交订单 → 跳转订单详情页
```

**4. 路由配置**

- `/rent/:vehicleId` - 租赁订单页面（需登录）

### 文件修改清单

#### 前端新建文件（2个）
1. `src/views/user/RentOrder.vue`
2. `src/api/user/order.js`

#### 前端修改文件（2个）
1. `src/router/index.js` - 添加租赁订单路由
2. `src/views/user/VehicleDetail.vue` - 修改"立即租赁"按钮跳转

## 验证方式
1. 未登录用户点击"立即租赁" → 跳转登录页
2. 已登录用户进入租赁订单页面
3. 选择时间后自动计算费用
4. 提交订单成功后跳转订单详情页
