# 管理端统计分析 - 订单分析

## 一、功能概述

在管理端新增"统计分析"菜单，包含"订单分析"子菜单，展示订单相关的统计图表，帮助管理员了解业务运营情况。

## 二、页面设计

### 2.1 页面布局

```
┌─────────────────────────────────────────────────────────┐
│  统计分析 > 订单分析                                      │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐               │
│  │  今日订单数      │  │  今日收入        │               │
│  │  128            │  │  ¥12,580        │               │
│  └─────────────────┘  └─────────────────┘               │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐│
│  │  订单趋势图                    [近7天] [近30天]      ││
│  │  📈 折线图                                          ││
│  └─────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────┤
│  ┌───────────────────┐  ┌───────────────────────────┐  │
│  │  订单状态分布       │  │  收入趋势图               │  │
│  │  🥧 饼图           │  │  📈 折线图 [近7天][近30天] │  │
│  │                    │  │                           │  │
│  └───────────────────┘  └───────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 统计卡片

| 卡片名称 | 数据来源 | 说明 |
|---------|---------|------|
| 今日订单数 | 今日创建的订单总数 | 实时统计 |
| 今日收入 | 今日已完成订单的总金额 | 实时统计 |

### 2.3 图表说明

#### 2.3.1 订单趋势图
- **类型**：折线图
- **数据**：按天统计订单数量
- **时间范围**：近7天 / 近30天（可切换）
- **X轴**：日期
- **Y轴**：订单数量

#### 2.3.2 订单状态分布
- **类型**：饼图
- **数据**：各状态订单数量占比
- **状态**：
  - 待支付 (pending)
  - 已支付 (paid)
  - 使用中 (in_use)
  - 已完成 (completed)
  - 已取消 (cancelled)

#### 2.3.3 收入趋势图
- **类型**：折线图
- **数据**：按天统计收入金额（已完成订单）
- **时间范围**：近7天 / 近30天（可切换）
- **X轴**：日期
- **Y轴**：收入金额（元）

## 三、后端设计

### 3.1 新增接口

| 接口 | 方法 | 路径 | 描述 |
|------|------|------|------|
| 订单趋势统计 | GET | `/api/admin/statistics/order-trend` | 按天统计订单数量 |
| 订单状态分布 | GET | `/api/admin/statistics/order-status` | 各状态订单数量 |
| 收入趋势统计 | GET | `/api/admin/statistics/revenue-trend` | 按天统计收入金额 |
| 今日概览 | GET | `/api/admin/statistics/today-overview` | 今日订单数和收入 |

### 3.2 接口详情

#### 3.2.1 订单趋势统计

**请求**：
```http
GET /api/admin/statistics/order-trend?days=7
```

**响应**：
```json
{
  "code": 200,
  "data": [
    { "date": "2024-01-15", "count": 12 },
    { "date": "2024-01-16", "count": 18 },
    { "date": "2024-01-17", "count": 15 },
    ...
  ]
}
```

#### 3.2.2 订单状态分布

**请求**：
```http
GET /api/admin/statistics/order-status
```

**响应**：
```json
{
  "code": 200,
  "data": [
    { "status": "待支付", "count": 5 },
    { "status": "已支付", "count": 8 },
    { "status": "使用中", "count": 12 },
    { "status": "已完成", "count": 156 },
    { "status": "已取消", "count": 23 }
  ]
}
```

#### 3.2.3 收入趋势统计

**请求**：
```http
GET /api/admin/statistics/revenue-trend?days=7
```

**响应**：
```json
{
  "code": 200,
  "data": [
    { "date": "2024-01-15", "amount": 2580.00 },
    { "date": "2024-01-16", "amount": 3200.00 },
    { "date": "2024-01-17", "amount": 1850.00 },
    ...
  ]
}
```

#### 3.2.4 今日概览

**请求**：
```http
GET /api/admin/statistics/today-overview
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "todayOrderCount": 28,
    "todayRevenue": 12580.00
  }
}
```

### 3.3 需新增文件

1. **StatisticsController.java** - 统计分析控制器
2. **StatisticsService.java** - 统计服务接口
3. **StatisticsServiceImpl.java** - 统计服务实现
4. **OrderTrendDTO.java** - 订单趋势数据DTO
5. **OrderStatusDTO.java** - 订单状态分布DTO
6. **RevenueTrendDTO.java** - 收入趋势数据DTO
7. **TodayOverviewDTO.java** - 今日概览DTO

## 四、前端设计

### 4.1 技术选型

使用 **ECharts** 作为图表库：
- 安装：`npm install echarts --save`
- 优点：功能强大、文档完善、社区活跃

### 4.2 需新增/修改文件

1. **src/views/admin/Statistics.vue** - 统计分析页面
2. **src/api/admin/statistics.js** - 统计接口API
3. **src/router/index.js** - 添加路由
4. **src/layouts/AdminLayout.vue** - 添加侧边栏菜单

### 4.3 路由配置

```js
{
  path: 'statistics',
  name: 'AdminStatistics',
  component: () => import('@/views/admin/Statistics.vue'),
  meta: { title: '订单分析' }
}
```

### 4.4 侧边栏菜单

```
车辆管理
订单管理
轮播图管理
用户管理
留言管理
统计分析 ← 新增
  └── 订单分析
```

## 五、开发步骤

### 5.1 后端
1. 创建 DTO 类（4个）
2. 创建 StatisticsService 接口
3. 创建 StatisticsServiceImpl 实现类
4. 创建 StatisticsController 控制器

### 5.2 前端
1. 安装 ECharts
2. 创建 API 接口文件
3. 创建统计分析页面
4. 添加路由配置
5. 修改侧边栏菜单

## 六、注意事项

1. 收入统计只计算"已完成"状态的订单
2. 今日数据按创建时间筛选（当日 00:00:00 - 23:59:59）
3. 趋势图数据按天分组，日期格式为 yyyy-MM-dd
4. 前端图表需要响应式适配
